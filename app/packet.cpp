#include "packet.h"

//// Construction / Destruction
//Packet::Packet() { }
//
//// Getters / Setters
//status Packet::getStatus() { return this->_status; }
//data Packet::getData() { return this->_data; }
//
//void Packet::setStatus(status st) { this->_status = status; }
//void Packet::setData(data dt) { this->_data = data; }